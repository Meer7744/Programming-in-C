#include <stdio.h> int main(){ 
int x; 
 printf("enter number to be checked"); scanf("%d",Cx); 
 
if (x%2==0){ printf("the number is even"); 
} 
else 
{ 
printf("the number is odd"); 
} 
return 0; 
} 
